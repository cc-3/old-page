server {
  listen 443;
  server_name cc-3.site;

  ssl on;
  ssl_certificate /etc/ssl/cc3.pem;
  ssl_certificate_key /etc/ssl/cc3.key;

  location / {
    proxy_set_header X-Forwarded-For $remote_addr;
    proxy_set_header Host $http_host;
    proxy_pass "http://localhost:3000";
  }
}
